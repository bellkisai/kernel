# Build artifacts
target/
*.exe
*.dll
*.so
*.dylib

# Cargo lock (library crate — don't commit)
# Note: for applications, Cargo.lock SHOULD be committed
# Since this is a library workspace, we exclude it
Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Environment
.env
.env.*
!.env.example

# Debug / profiling
*.pdb
*.dSYM/
perf.data
flamegraph.svg

# Test artifacts
*.profraw
coverage/

# Echo Memory data (local testing)
.bellkis-kernel/

# Model cache (fastembed downloads)
# Committed separately or bundled in release
.fastembed_cache/
**/.fastembed_cache/
benchmarks/LongMemEval/
benchmarks/hindsight-benchmarks/

# Node.js / Frontend (shrimpk-viz)
node_modules/
**/node_modules/
**/dist/
*.tsbuildinfo

# Python bytecode
__pycache__/
*.pyc
*.pyo

# ONNX model files
*.onnx

# Claude Code project instructions (local only)
CLAUDE.md

# Internal sprint research docs (belong in docs-repo, not public kernel)
docs/KS*
docs/S*-*

# ShrimPK local data
.shrimpk/
*.shrm

# Wix installer artifacts
*.msi
*.wixobj
*.wixpdb

# Lock files from model downloads (Cargo.lock already excluded above)
*.lock
